# Уведомления

## Что это

**Колокольчик** в правом верхнем углу — центр уведомлений: красная
циферка показывает количество непрочитанных, клик открывает панель
со списком.

Через колокольчик прилетают:
- упоминания в базе знаний (`@vasya` в комментарии или странице);
- ответы в тредах базы знаний;
- личные поздравления с днём рождения;
- предупреждения об истечении медкнижки;
- welcome-нотификация после принятия приглашения;
- системные сообщения.

Те же уведомления можно дополнительно получать как **push в браузер** —
нативные плашки ОС, которые приходят, даже когда вкладка приложения
закрыта (см. раздел «Push-уведомления в браузер» ниже). По умолчанию
выключены, включаются кнопкой.

## Для кого / когда использовать

- **Все пользователи** видят свой колокольчик с уведомлениями,
  релевантными им лично.
- **Сотрудники** получают: упоминания в базе знаний, поздравления
  с ДР (для них самих), напоминание о медкнижке (для них самих),
  ответы в комментариях, обязательные к прочтению страницы.
- **Руководители** дополнительно — heads-up за 7 дней до ДР коллег
  («через неделю у XYZ день рождения, успей подготовить подарок»).

## Как пользоваться

1. **Открыть** — клик на колокольчик. Панель появляется снизу-справа
   от иконки.
2. **Иконки в шапке панели** (слева направо, с подсказками при
   наведении):
   - **Прочитать все** — помечает все уведомления прочитанными.
   - **Архивировать прочитанные** — убирает прочитанные в архив.
   - **Фильтр** — выбор, что показывать: «Непрочитанные и
     прочитанные» / «Только непрочитанные» / «Архив».
   - **Настройки** (шестерёнка) — управление push-уведомлениями
     (см. ниже).
3. **Непрочитанные** помечены синей точкой справа от даты.
4. **Клик на уведомление** — открывает связанный объект
   (страницу базы знаний, профиль для медкнижки и т. д.) и помечает
   как прочитанное.
5. **Наведение на строку** → справа (вместо даты) появляются две
   кнопки: **«Отметить прочитанным»** и **«В архив»**. В режиме
   «Архив» — кнопка **«Восстановить»**.

## Push-уведомления в браузер

Чтобы получать уведомления, **даже когда приложение закрыто**, включите
push. Два пути:

- **При первом заходе** под верхней панелью появляется баннер
  «Включите уведомления» — нажмите «Включить» (можно «Не сейчас»).
- **В любой момент** — колокольчик → **шестерёнка (Настройки)** →
  переключатель «На этом устройстве».

Браузер спросит разрешение — нажмите «Разрешить». После этого те же
уведомления, что прилетают в колокольчик, дублируются нативной плашкой
ОС; клик по плашке открывает связанный объект.

- **Где работает:** desktop Chrome / Firefox / Edge и Android Chrome.
- **Отключить:** там же в настройках (шестерёнка) выключите
  переключатель. Можно также заблокировать уведомления в настройках
  самого браузера для сайта.
- Push включается **на каждом устройстве/браузере отдельно** —
  разрешение привязано к конкретному браузеру.

### iPhone / iPad

На iOS push работает **только из установленного приложения** (iOS 16.4+).
В обычной вкладке Safari кнопки «Включить» не будет — вместо неё
колокольчик покажет подсказку. Порядок:

1. Откройте сайт в **Safari** (не в стороннем браузере).
2. Нажмите кнопку **«Поделиться»** (квадрат со стрелкой вверх) →
   **«На экран „Домой“»** → «Добавить».
3. Откройте приложение уже **с иконки на экране „Домой“** (не из Safari).
4. Колокольчик → **«Включить»** → «Разрешить».

После этого push приходят как на обычном приложении, даже когда оно
закрыто.

## Какие уведомления приходят

### База знаний

- **Упоминание в странице** — кто-то написал `@you` в KB-странице.
- **Упоминание в комментарии** — то же, но в комментарии.
- **Ответ в треде** — кто-то ответил в треде, где вы писали.
- **Обязательно к прочтению** — админ отметил страницу как
  required для вашей роли.

### Команда

- **С днём рождения!** — приходит ВАМ в день вашего ДР. Текст
  сгенерирован AI (DeepSeek), уникальный — каждый год новое
  поздравление. Если AI недоступен — fallback на статический рандом
  из 3 вариантов.
- **Скоро ДР коллеги** — приходит коллегам за 7 дней до ДР сотрудника
  заведения.
- **Скоро истекает медкнижка** — приходит ВАМ за 30 дней до конца
  срока действия медкнижки. Каждый раз когда срок меняется на более
  близкий — уведомление прилетает заново.
- **Добро пожаловать!** — приходит сразу после принятия приглашения,
  если вы новый пользователь. Подсказка заполнить профиль (фото,
  телефон, telegram, дата рождения).

## FAQ / частые ошибки

- **Q:** Видел уведомление, кликнул — оно пометилось как прочитанное,
  но я хочу вернуть его в непрочитанные.
  **A:** Сейчас такой возможности нет — прочитанное остаётся
  прочитанным.

- **Q:** Слишком много уведомлений в архиве, как чистить?
  **A:** Архив автоматически чистится: уведомления старше 30 дней
  удаляются ночным cron'ом (`/api/cron/auto-archive-notifications`).
  Также можно «Архивировать прочитанные» одной кнопкой.

- **Q:** Не приходит поздравление с ДР, хотя сегодня моё ДР.
  **A:** Cron работает по московскому времени (`Europe/Moscow`).
  Поздравление прилетает в течение часа после полуночи МСК. Если не
  пришло — проверьте, что в `/profile` дата рождения заполнена и
  сегодня — её день+месяц.

## Известные ограничения

- Push в браузер есть на desktop и Android; на iPhone/iPad — только
  после установки приложения «на экран Домой» (PWA).
- Push дублирует колокольчик целиком — отдельно выбрать, какие
  категории слать в push, пока нельзя (только включить/выключить всё).
- Звуковых уведомлений нет.
- Email-дублирование уведомлений не настроено.
- Нельзя пометить уведомление как непрочитанное обратно.
- Нельзя «mute» отдельные категории (только фильтр на просмотр).

## Где в коде

- `src/app/(dashboard)/notifications/` — страница со списком +
  server actions
- `src/components/shared/notification-bell.tsx` — кнопка колокольчика
- `src/components/shared/notification-row.tsx` — рендер строки
- `src/lib/notifications/registry.ts` — список known-типов с иконками,
  цветами, verb'ами
- `supabase/migrations/140_birthday_notifications.sql` — cron logic
  для ДР
- `supabase/migrations/135_medical_book_expiry_notifications.sql` —
  cron logic для медкнижки
- `src/app/api/cron/birthday-notifications/route.ts`
- `src/app/api/cron/medical-book-expiry-notifications/route.ts`
- `src/app/api/cron/auto-archive-notifications/route.ts`
- `src/app/api/cron/push-dispatch/route.ts` — рассылка Web Push по
  неразосланным уведомлениям (claim по `notifications.pushed_at`)
- `src/lib/push/` — VAPID-инициализация + отправка (`web-push`)
- `src/hooks/use-push-subscription.ts` — подписка устройства на клиенте
- `src/components/shared/notification-settings.tsx` — поповер ⚙ с push-Switch
- `src/components/shared/push-prompt-banner.tsx` — баннер первого захода
- `src/app/(dashboard)/notifications/push-actions.ts` — save/delete подписки
- `src/app/manifest.ts` + `public/sw.js` — PWA-манифест и сервис-воркер
- `src/app/apple-icon.tsx` + `src/app/api/icons/[variant]/route.tsx` +
  `src/lib/icons.tsx` — apple-touch-icon (iOS) и PNG-иконки manifest
  (Android), генерятся через next/og
- `src/middleware.ts` — `sw.js` / `manifest.webmanifest` / `apple-icon`
  исключены из auth-редиректа (тянутся анонимно)
- `supabase/migrations/214_push_subscriptions.sql` — таблица подписок +
  `notifications.pushed_at`
